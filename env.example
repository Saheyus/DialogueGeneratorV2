# Configuration des variables d'environnement pour DialogueGenerator
# Copier ce fichier vers .env et remplir les valeurs réelles
# ⚠️ NE JAMAIS COMMITTER le fichier .env avec des secrets réels

# ============================================
# VARIABLES REQUISES EN PRODUCTION
# ============================================

# Environnement (development ou production)
# En production, doit être défini à "production"
ENVIRONMENT=development

# Clé secrète pour signer les tokens JWT
# ⚠️ EN PRODUCTION : Générer une clé forte avec :
# python -c "import secrets; print(secrets.token_urlsafe(32))"
# Ne JAMAIS utiliser la valeur par défaut en production
JWT_SECRET_KEY=your-secret-key-change-in-production

# Clé API OpenAI (requis pour la génération de dialogues)
OPENAI_API_KEY=your-openai-api-key-here

# Clé API Notion (requis pour la synchronisation du vocabulaire et des guides)
# Obtenir depuis https://www.notion.so/my-integrations
NOTION_API_KEY=your-notion-api-key-here

# ============================================
# CONFIGURATION CORS (Production uniquement)
# ============================================

# Origines CORS autorisées (format CSV, séparées par des virgules)
# En développement, laisser vide pour autoriser toutes les origines (*)
# En production, lister uniquement les domaines autorisés
# Exemple: https://votre-domaine.com,https://www.votre-domaine.com
CORS_ORIGINS=

# ============================================
# CONFIGURATION DES CHEMINS GDD (Optionnel)
# ============================================

# Chemin vers le répertoire des catégories GDD (fichiers JSON)
# Si non défini, utilise le chemin par défaut : DialogueGenerator/data/GDD_categories/
# Exemple: /serveur/app/data/GDD_categories
# GDD_CATEGORIES_PATH=

# Chemin vers le répertoire contenant Vision.json (ou directement le fichier Vision.json)
# Si non défini, utilise le chemin par défaut : DialogueGenerator/data/Vision.json
# Exemple: /serveur/app/data (pour chercher Vision.json dans ce dossier)
# Exemple: /serveur/app/data/Vision.json (chemin direct vers le fichier)
# GDD_IMPORT_PATH=

# ============================================
# CONFIGURATION API (Optionnel)
# ============================================

# Port sur lequel l'API écoute (défaut: 4242)
# API_PORT=4242

# ============================================
# CONFIGURATION RATE LIMITING (Optionnel)
# ============================================

# Activer le rate limiting sur les endpoints d'authentification (défaut: true)
# AUTH_RATE_LIMIT_ENABLED=true

# Nombre de requêtes autorisées par fenêtre (défaut: 5)
# AUTH_RATE_LIMIT_REQUESTS=5

# Fenêtre de temps en secondes (défaut: 60)
# AUTH_RATE_LIMIT_WINDOW=60

# ============================================
# CONFIGURATION FRONTEND (Build uniquement)
# ============================================

# URL de base de l'API pour le build du frontend
# Si l'API est sur le même domaine que le frontend, laisser vide (utilise des URLs relatives)
# Si l'API est sur un autre domaine, définir l'URL complète
# Exemple: https://api.votre-domaine.com
# VITE_API_BASE_URL=


---
description: UI (PySide6) — réactivité, qasync, tests pytest-qt — ⚠️ DÉPRÉCIÉ
globs: ["ui/**/*.py", "main_app.py", "launcher.py"]
alwaysApply: false
---

⚠️ **DÉPRÉCIÉ** : L'interface desktop PySide6 est dépréciée. Utiliser l'interface web React (`npm run dev`) qui est l'interface principale et maintenue.

Cette règle s'applique uniquement pour la maintenance de l'ancienne UI desktop (si nécessaire) ou pour les tests pytest-qt.

- PySide6: préserver la réactivité (pas de travail long sur le thread UI). Pour async: s'aligner sur `qasync`/patterns existants.
- Les erreurs côté UI doivent être loggées + présentées proprement (message utilisateur non-technique).
- Tests UI: pytest-qt (`qtbot`), pas d'accès réseau, et éviter les dépendances aux données GDD externes non contrôlées.

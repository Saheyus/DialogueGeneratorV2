# Spécifications UX - Features V1.0

**Date:** 2026-01-14  
**Auteur:** Sally (UX Designer)  
**Statut:** Prêt pour implémentation

## Vue d'ensemble

Ce document décrit les spécifications UX détaillées pour deux features prioritaires de la V1.0 :
1. **Progress Feedback pendant Génération** (Priority 1)
2. **Presets de Configuration Sauvegardables** (Priority 2)

---

## 1. Progress Feedback pendant Génération

### Objectif

Donner un feedback clair et détaillé à l'utilisateur pendant la génération d'un dialogue, réduisant l'anxiété d'attente et permettant d'interrompre si nécessaire.

### Emplacement UI

**Intégration dans le panneau "Détails" (panneau de droite)**, dans l'onglet "Dialogue généré" ou un nouvel onglet dédié "Génération".

### Composants UI

#### 1.1 Zone de Progression (toujours visible pendant génération)

**Layout :**
```
┌─ Génération en cours ────────────────────┐
│                                          │
│  ✅ Contexte chargé (2.3s)               │
│  ✅ Prompt construit (39 284 tokens)     │
│  ⏳ LLM thinking... (15s elapsed)        │
│  ⏸️  Génération réponse...               │
│  ⏸️  Validation structure...             │
│                                          │
│  ████████████░░░░░░░░░░ 60%             │
│  Temps estimé restant : ~20s             │
│                                          │
│  [ 🛑 Interrompre ]  [ Arrière-plan → ] │
└──────────────────────────────────────────┘
```

**États des étapes :**
- **✅ Complété** : Checkmark vert, texte gris, durée affichée
- **⏳ En cours** : Spinner animé, texte actif, timer en cours
- **⏸️ En attente** : Icône pause, texte grisé

**Progress Bar :**
- Barre de progression visuelle (0-100%)
- Couleur : Bleu pendant progression normale
- Estimation de temps basée sur historique (si disponible)

**Boutons d'action :**
- **🛑 Interrompre** : Arrête immédiatement la génération, revient à l'état initial
- **Arrière-plan →** : Minimise la génération, permet de continuer à naviguer (notifications toast)

#### 1.2 Logs Verbose (mode détaillé)

**Section dédiée avec scroll :**
```
┌─ Logs de Génération ─────────────────────┐
│                                          │
│  [12:34:56] Début construction contexte │
│  [12:34:58] Contexte chargé : 39.2K tok │
│  [12:34:59] Appel API OpenAI...          │
│  [12:35:02] Réception thinking tokens... │
│  [12:35:15] Thinking complété            │
│  [12:35:16] Génération réponse...        │
│  [12:35:42] Réponse reçue (2.1K tokens)  │
│  [12:35:43] Validation structure...      │
│  [12:35:44] ✅ Validation réussie        │
│                                          │
└──────────────────────────────────────────┘
```

**Caractéristiques :**
- **Format** : `[HH:MM:SS] Message`
- **Auto-scroll** : Scroll automatique vers le bas à chaque nouveau log
- **Couleurs** : 
  - Info : Texte gris
  - Succès : Texte vert
  - Erreur : Texte rouge
  - Warning : Texte orange
- **Toggle** : Bouton pour masquer/afficher les logs (collapsible)

#### 1.3 Notifications (génération en arrière-plan)

**Toast notification** (apparition en haut à droite) :
```
┌────────────────────────────────────┐
│ ✅ Génération terminée             │
│ "Dialogue généré avec succès"      │
│ [Voir le résultat] [Ignorer]       │
└────────────────────────────────────┘
```

**États :**
- **Succès** : Toast vert, icône ✅
- **Erreur** : Toast rouge, icône ❌, message d'erreur clair
- **Interrompue** : Toast orange, icône ⚠️

**Actions :**
- Clic "Voir le résultat" : Ouvre le panneau Détails avec le résultat
- Auto-dismiss après 10 secondes (avec animation)

#### 1.4 États d'Erreur

**Affichage dans la zone de progression :**
```
┌─ Erreur pendant Génération ────────────┐
│                                          │
│  ✅ Contexte chargé                     │
│  ✅ Prompt construit                    │
│  ❌ Erreur LLM : Timeout API            │
│                                          │
│  Message : L'API OpenAI n'a pas         │
│  répondu dans les délais (30s)          │
│                                          │
│  [ 🔄 Réessayer ]  [ ✕ Fermer ]        │
└──────────────────────────────────────────┘
```

**Messages d'erreur clairs :**
- Timeout API : "L'API LLM n'a pas répondu dans les délais. Vérifiez votre connexion."
- Erreur réseau : "Connexion perdue. Vérifiez votre connexion internet."
- Erreur validation : "La structure générée est invalide. Le LLM a produit un format incorrect."
- Token limit : "Le prompt dépasse la limite de tokens. Réduisez le contexte."

### Workflow Utilisateur

1. **Clic "Générer"**
   - La zone de progression apparaît dans le panneau Détails
   - Les logs commencent à s'afficher
   - Le bouton "Générer" devient désactivé

2. **Pendant la génération**
   - Les étapes s'actualisent en temps réel
   - Les logs s'ajoutent au fur et à mesure
   - Le progress bar progresse
   - Estimation de temps mise à jour

3. **Actions possibles**
   - **Interrompre** : Arrête tout, revient à l'état initial
   - **Arrière-plan** : Génération continue, notification à la fin

4. **Fin de génération**
   - Si visible : Résultat affiché dans l'onglet "Dialogue généré"
   - Si arrière-plan : Toast notification + résultat disponible

### Backend Requirements

**Endpoints nécessaires :**
- WebSocket ou Server-Sent Events (SSE) pour streaming des logs
- Endpoint pour interrompre une génération : `POST /api/v1/dialogues/{id}/cancel`
- Stockage temporaire des logs de génération (session)

**Événements à streamer :**
- `context_loaded` (durée, tokens)
- `prompt_built` (tokens)
- `llm_thinking_start`
- `llm_thinking_progress` (tokens reçus)
- `llm_thinking_complete`
- `response_generating`
- `response_progress` (tokens)
- `validation_start`
- `validation_complete`
- `generation_complete` (résultat)
- `generation_error` (message d'erreur)

---

## 2. Presets de Configuration

### Objectif

Permettre aux utilisateurs de sauvegarder des configurations fréquentes (personnages, lieux, instructions) pour les réutiliser en un clic.

### Emplacement UI

**Dropdown "Charger un preset"** en haut de la page "Génération de Dialogues", à côté du titre.

### Composants UI

#### 2.1 Dropdown "Charger un preset"

**Position :** En haut à gauche, avant le titre "Génération de Dialogues"

**Layout fermé :**
```
┌─ [📋 Charger un preset ▼] ──────────────────────┐
```

**Layout ouvert (dropdown) :**
```
┌─ Presets ───────────────────────────────────────┐
│                                                  │
│  🏛️  Léviathan                                  │
│     Rencontre diplomatique (3 persos, 2 lieux)  │
│                                                  │
│  ⚔️  Combat urbain                              │
│     Cendreveine (5 persos, 3 lieux)             │
│                                                  │
│  🔮 Révélation mystique                         │
│     Nef Centrale (2 persos, 1 lieu)             │
│                                                  │
│  ────────────────────────────────────────────   │
│  ➕ Créer un nouveau preset                     │
│                                                  │
└──────────────────────────────────────────────────┘
```

**Groupes de presets :**
- **Par région** : Tous les presets liés à "Léviathan pétrifié" groupés sous "🏛️ Léviathan"
- **Par type de scène** : Presets de "Confrontation", "Révélation", etc.
- **Génériques** : Presets réutilisables partout (pas de région spécifique)

**Format de chaque item :**
- **Ligne 1** : Emoji + Nom du preset (gras)
- **Ligne 2** : Preview courte (nombre d'entités, type)

#### 2.2 Modal "Sauvegarder un preset"

**Trigger :** Bouton "Sauvegarder comme preset" (nouveau bouton dans la barre d'actions)

**Layout :**
```
┌─ Sauvegarder un preset ─────────────────────────┐
│                                                  │
│  Nom du preset *                                │
│  ┌────────────────────────────────────────────┐ │
│  │ Rencontre diplomatique Léviathan           │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  Description (optionnel)                        │
│  ┌────────────────────────────────────────────┐ │
│  │ Première rencontre avec Uresaïr dans la    │ │
│  │ Nef Centrale                                │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  Icône (emoji)                                  │
│  🏛️ [Choisir autre emoji]                      │
│                                                  │
│  Aperçu de la configuration :                   │
│  ┌────────────────────────────────────────────┐ │
│  │ Personnages (2) :                           │ │
│  │   • Seigneuresse Uresäir                    │ │
│  │   • Akthar-Neth Amatru                      │ │
│  │                                              │ │
│  │ Lieux (2) :                                  │ │
│  │   • Léviathan pétrifié                      │ │
│  │   • Nef Centrale                            │ │
│  │                                              │ │
│  │ Type de scène : Première rencontre          │ │
│  │ Instructions personnalisées : Oui           │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  [Annuler]              [💾 Sauvegarder]        │
└──────────────────────────────────────────────────┘
```

**Champs :**
- **Nom** : Obligatoire, max 50 caractères
- **Description** : Optionnel, max 200 caractères
- **Icône** : Emoji picker simple (liste de 20-30 emojis courants)
- **Aperçu** : Lecture seule, montre ce qui sera sauvegardé

**Validation :**
- Nom requis
- Nom unique (erreur si doublon)
- Aperçu doit contenir au moins 1 élément (sinon preset inutile)

#### 2.3 Gestion des Presets (optionnel V1.0)

**Panneau dédié** accessible via menu "Configuration" → "Mes presets"

**Layout :**
```
┌─ Mes Presets ───────────────────────────────────┐
│                                                  │
│  🏛️  Léviathan                                  │
│     ┌──────────────────────────────────────┐   │
│     │ Rencontre diplomatique               │   │
│     │ 2 persos, 2 lieux, Première rencontre│   │
│     │ [✏️ Renommer] [📋 Dupliquer] [🗑️]    │   │
│     └──────────────────────────────────────┘   │
│     ┌──────────────────────────────────────┐   │
│     │ Conversation mystique                │   │
│     │ 1 perso, 1 lieu, Révélation          │   │
│     │ [✏️ Renommer] [📋 Dupliquer] [🗑️]    │   │
│     └──────────────────────────────────────┘   │
│                                                  │
│  ⚔️  Cendreveine                                │
│     ... (même structure)                        │
│                                                  │
└──────────────────────────────────────────────────┘
```

**Actions :**
- **Renommer** : Modal avec input
- **Dupliquer** : Crée une copie (nom + "- Copie")
- **Supprimer** : Confirmation avant suppression

**Note :** Cette section peut être en V1.1 si on veut limiter le scope V1.0

### Workflow Utilisateur

#### Charger un preset

1. Clic sur "Charger un preset"
2. Dropdown s'ouvre, liste des presets groupés
3. Sélection d'un preset
4. **Tous les champs se remplissent automatiquement :**
   - Personnages sélectionnés
   - Lieux sélectionnés
   - Type de scène
   - Instructions de scène (si sauvegardées)
   - Auteur/Système LLM (si sauvegardés)
5. L'utilisateur peut ajuster avant de générer

#### Sauvegarder un preset

1. Configuration de la scène (personnages, lieux, instructions, etc.)
2. Clic sur "Sauvegarder comme preset"
3. Modal s'ouvre
4. Remplissage du nom, description (optionnel), choix de l'icône
5. Vérification de l'aperçu
6. Clic "Sauvegarder"
7. Le preset apparaît dans le dropdown

### Données Sauvegardées

**Structure d'un preset :**
```json
{
  "id": "uuid",
  "name": "Rencontre diplomatique Léviathan",
  "description": "Première rencontre avec Uresaïr...",
  "icon": "🏛️",
  "category": "Léviathan", // Auto-déterminé par région
  "created_at": "2026-01-14T12:00:00Z",
  "config": {
    "characters": ["Seigneuresse Uresäir", "Akthar-Neth Amatru"],
    "locations": ["Léviathan pétrifié", "Nef Centrale"],
    "scene_type": "Première rencontre",
    "scene_brief": "...", // Optionnel
    "author_instructions": "...", // Optionnel
    "system_llm_prompt": "..." // Optionnel
  }
}
```

**Note :** Pour V1.0, on peut simplifier en ne sauvegardant que les entités (personnages/lieux) et le type de scène. Les instructions personnalisées peuvent être en V1.1.

### Backend Requirements

**Endpoints nécessaires :**
- `GET /api/v1/presets` : Liste tous les presets de l'utilisateur (groupés par catégorie)
- `GET /api/v1/presets/{id}` : Récupère un preset spécifique
- `POST /api/v1/presets` : Crée un nouveau preset
- `PUT /api/v1/presets/{id}` : Met à jour un preset
- `DELETE /api/v1/presets/{id}` : Supprime un preset

**Storage :**
- Fichier JSON par utilisateur : `data/presets/{user_id}.json`
- Ou fichier global : `data/presets.json` (array de presets avec user_id)

**Validation :**
- Nom unique par utilisateur
- Validation des entités référencées (vérifier qu'elles existent encore dans le GDD)
- Cleanup si entités supprimées (warning ou suppression automatique du preset)

---

## Priorités d'Implémentation

### MVP (Minimum Viable)

**Progress Feedback :**
- ✅ Zone de progression basique (étapes + progress bar)
- ✅ Bouton Interrompre
- ✅ Messages d'erreur clairs
- ⏸️ Logs verbose (V1.0.1)
- ⏸️ Génération en arrière-plan (V1.0.1)

**Presets :**
- ✅ Dropdown charger preset
- ✅ Modal sauvegarder preset
- ✅ Sauvegarde/chargement basique
- ⏸️ Gestion des presets (renommer/dupliquer/supprimer) (V1.0.1)

### V1.0.1 (Améliorations rapides)

- Logs verbose détaillés
- Génération en arrière-plan
- Gestion complète des presets

---

## Notes Techniques

### Performance

- **Presets** : Chargement rapide (<100ms), pas d'impact sur le chargement initial
- **Progress Feedback** : WebSocket/SSE pour streaming, éviter polling

### Accessibilité

- **Keyboard navigation** : Tab pour naviguer dans les modals, Enter pour valider, Esc pour fermer
- **Screen readers** : Labels appropriés, aria-live pour les logs de progression
- **Focus management** : Focus sur le bouton "Interrompre" pendant génération (pour accessibilité clavier)

### Mobile/Responsive

- **Progress Feedback** : Responsive, adapté aux petits écrans (logs scrollables)
- **Presets** : Dropdown responsive (full-width sur mobile), modal centré

---

## Références

- PRD : `_bmad-output/planning-artifacts/prd.md`
- Architecture API : `docs/architecture-api.md`
- API Contracts : `docs/api-contracts-api.md`
